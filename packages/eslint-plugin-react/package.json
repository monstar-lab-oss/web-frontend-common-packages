{
  "name": "@monstarlab/eslint-plugin-react",
  "version": "0.0.0",
  "description": "Eslint rules for ML React project",
  "license": "MIT",
  "main": "./dist/index.js",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "start": "yarn build -w",
    "prebuild": "rimraf dist",
    "build": "tsc -p .",
    "prepack": "yarn build"
  },
  "dependencies": {
    "eslint-config-airbnb-typescript-prettier": "^4.2.0",
    "eslint-import-resolver-typescript": "^2.5.0",
    "eslint-plugin-filenames": "^1.3.2"
  },
  "peerDependencies": {
    "eslint": "^5.16.0 || ^6.8.0 || ^7.2.0",
    "prettier": "^1.18.2 || ^2.0.0",
    "typescript": ">=3.3.1"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
